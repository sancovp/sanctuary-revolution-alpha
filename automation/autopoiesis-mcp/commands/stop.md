---
description: "Cancel active autopoiesis loop"
---

# Autopoiesis Stop

Run this command to cancel the active loop:

```
"$CLAUDE_PLUGIN_ROOT/scripts/stop-autopoiesis.sh"
```

This archives the current promise to `/tmp/promise_history/` and stops the loop.
