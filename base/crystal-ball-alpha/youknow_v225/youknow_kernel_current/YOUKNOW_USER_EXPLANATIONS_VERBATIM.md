# YOUKNOW User Explanations (Verbatim)

Tied spec by name: `YOUKNOW_WORKING_SPEC_SYNTHESIS.md`

This file preserves user explanations verbatim from the conversation and anchors them to the working spec above.

---

> "the compilation aspect comes from continually running EMR while an LLM is on the other side."

> "why does it need IR when EMR is a shacl process?"

> "and what is that for, functionally, in terms of what we're trying to do with the LLM agent?"

> "Strong compression == have MSC with justifies on every relationship in derivationchain of entity"

> "Weak compression == not have MSC with justifies on every relationship in derivationchain of entity"

> "and so when strong compression, we admit that labeled rel into ONT from SOUP thats literally the way it works. So the same for any proper entity that isnt a rel, when its subgraph is all justified, it becomes strong and is admitted to ONT"

> "so it gets admitted on that condition PLUS the CatOfCat superclass chain is *declared bounded*"

> "exactly so if we could just have that process working, that's called compilation. When an LLM can REPL with YOUKNOW shell and actually just *say whatever, then progressively reify it into programs relationships everywhere*"

> "programs MEANS EXACTLY that it was strengthened"

> "And when it is strengthened we know to an SES layer degree which shows how many layers down it is typed, as a subgraph, without arbitrary str"

> "And so we know it is literally programmed"

> "right the key here is 4. we can progressively templatize everything from the bottom up this way"

> "and with codegen it would mean it gets simulated"

> "well we dont wanna change the way its returning the errors. do you see how that works? the reasoner errors/shacl errors etc?"

> "so it goes \"i dont know what this is a, so therefore i need an ABCD chain for this\"? that's the first step?"

> "yeah so i will explain it: we build out the hologram and lift the isomorphism over and over again so we have the whole stack of isomorphisms from all the different perspectives. These become like vectors to the LLM and it can project into a collapsed space - a point. Then you get a nice label, the feature is typed and not hypergraphic."

> "SES type depth layers should measure how many args are on a constructor and how deeply you have to drill them to hit arbitrary string. That's it."

---

Reference:
- `YOUKNOW_WORKING_SPEC_SYNTHESIS.md`
