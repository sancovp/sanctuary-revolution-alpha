# SESSION HANDOFF - CAVE Architecture Crystallization
## Date: 2026-01-20 (UPDATED AT 90% CONTEXT)

## COMPLETED THIS SESSION:

### 1. Config Snapshots System
**File:** `/tmp/cave/cave/core/config_snapshots.py`
- Auto-snapshot on startup/shutdown
- Lineage tracking (which base config)
- Diff within lineage to avoid duplicates
- Official/saved/snapshots directories
- `ConfigSnapshotManager` class ready to use

### 1b. Config Snapshots WIRED INTO CAVEAgent
- `cave_agent.py` now imports ConfigSnapshotManager
- Auto-snapshots on startup via `_snapshot_on_startup()`
- `snapshot_on_shutdown()` called from http_server shutdown event
- New methods: `get_config_info()`, `list_configs()`, `save_config(name)`

### 1c. HTTP Endpoints Added
- `GET /configs` - list all configs
- `GET /configs/active` - get active config info
- `POST /configs/save` - save named config
- Shutdown event calls `cave.snapshot_on_shutdown()`

### 2. SmartGrug v1 Config
**File:** `/tmp/heaven_data/configs/official/smartgrug_v1.json`
- First official persona config
- Content automation focused
- MCPs: obs-mcp, discord-mcp, content-pipeline

## KEY ARCHITECTURAL DECISIONS:

### 1. One App (SancRev) - Progressive Reveal via Tier
- CAVE tier = SmartGrug only, PAIAB/SANCTUM features disabled (return upgrade error)
- PAIAB tier = + Sophia
- SANCTUM tier = + GNOSYS (orchestrates others)

### 2. Agent Types
- **SmartGrug** = CAVEAgent (simple, content automation)
- **PAIA** = CAVEAgent + OMNISANC + gnosys_kit
- **SancRevAgent** = PAIA + nested agents (GNOSYS)

### 3. Persona as MCP
- Each persona callable via MCP wrapper
- `call_smartgrug(task)` routes to CAVEAgent session
- GNOSYS can call Sophia + SmartGrug as MCPs

### 4. Content Pipeline - 95% Done
- JourneyCore model: `/tmp/cave_discord_fork/core.py`
- All renderers: `/tmp/cave_discord_fork/renderers.py`
- ContentSuite.render_all() works
- **MISSING:** wire CAVEDiscordPoster channel ID parsing (~20 lines)

## NEXT STEPS:

1. Wire config_snapshots into CAVEAgent startup/shutdown
2. Create Sophia config (PAIAB tier)
3. Create GNOSYS config (SANCTUM tier) with nested agent calls
4. Fix CAVEDiscordPoster channel ID parsing in `/tmp/cave_discord_fork/discord_mcp_client.py`
5. Create MCP wrappers for persona calling

## KEY FILES:
- `/tmp/cave/cave/core/config_snapshots.py` (NEW - config versioning)
- `/tmp/heaven_data/configs/official/smartgrug_v1.json` (NEW - first persona)
- `/tmp/cave_discord_fork/` (content pipeline - JourneyCore, renderers)
- `/tmp/obs-mcp-cave/` (OBS + marks + ffmpeg)
- `/tmp/cave/cave/core/cave_agent.py` (main agent runtime)
- `/tmp/cave/cave/server/http_server.py` (FastAPI endpoints)

## BUSINESS MODEL CONFIRMED:
- Open source code, tier-gated features via stubs
- One codebase, one Docker image
- User configs saved/swapped via ConfigSnapshotManager
